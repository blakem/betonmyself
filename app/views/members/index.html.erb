You made it to the fancy happy members page...
 <%= render :active_scaffold => :bet, 
            :constraints => { :user_id => @user.id } 
  %>
 <%= render :active_scaffold => :accomplishments, 
            :constraints => { :user_id => @user.id } 
  %>
 <p><%= link_to "Log Out", :controller => "logout" %></p>
