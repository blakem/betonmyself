 <%= render :active_scaffold => :deleted_accomplishments, 
            :constraints => { :user_id => current_user.id, :state => BomConstant::BET_STATE_DELETED }
  %>

<p><%= link_to "Show Current Goals", '/' -%></p>
